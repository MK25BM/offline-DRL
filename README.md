# offline-DRL
The provided Python code implements a comprehensive offline reinforcement learning (RL) pipeline, focusing on a simulated Type 1 Diabetes (T1D) environment. The pipeline covers environment setup, data collection, dataset management, offline RL algorithm configuration, training, evaluation, and model persistence.
